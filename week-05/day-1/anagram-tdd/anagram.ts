'use strict';

export function anagramCompare(){
  let anagramBase: string = 'listen';
  let anagramModded: string = 'silent';

  function anagramCompare(quote1: string, quote2: string): boolean {
    return (quote1 === quote2);
  }
}
